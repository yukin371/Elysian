# 2026-04-28 若依基础功能对齐矩阵

## 目标

- 用一份矩阵明确 `Elysian` 当前与“若依常用后台基础功能”之间的对齐程度。
- 固定当前优先级：先补后台基础能力与工作区闭环，再考虑继续外扩 workflow、Studio 或更重的平台能力。
- 避免把“已有后端接口”误判成“后台基础功能已经完整可用”。

## 适用边界

本文件只用于当前一轮优先级判断，覆盖：

- `apps/server` 的标准系统模块与 auth 安全基线
- `apps/example-vue` 的企业工作区装配与真实后台交互
- `packages/persistence` / `packages/schema` 中支撑上述模块的既有 owner

本文件不负责：

- 重写 `Phase 7` 之后的长期 PRD
- 定义 `generator / SQL / workflow` 的详细实施设计
- 把若依的所有扩展能力都拉进当前主线

## 对标口径

这里的“若依基本功能”只指企业后台最常用、最先被用户感知的一层能力：

- 系统管理基础模块是否闭环可用
- 登录、权限、在线会话、日志与导入导出是否具备日常使用形态
- 前端是否像一个可切换、可操作、可验证的后台，而不只是若干演示页

不要求：

- 技术栈一致
- 完整复刻若依全部模块
- 提前进入工作流扩展、低代码平台、消息中心或 SSO 体系

## 状态说明

- `✅ 已完成`：已有实现，且当前已形成可验证闭环
- `🟡 部分完成`：已有后端或前端切片，但离“后台可用”还差关键环节
- `⚪ 未开始`：尚未进入实现
- `⛔ 当前不优先`：已有规划，但本轮不抢占第一优先级

## 功能矩阵

| 能力域 | 若依常见基线 | 当前状态 | 真实情况 | 证据 owner | 当前缺口 |
|---|---|---|---|---|---|
| 用户 / 角色 / 菜单 / 部门 | 基础系统管理四件套 | 🟡 部分完成 | 后端 CRUD 已闭环，示例前端已有对应工作区 | `apps/server` + `apps/example-vue` | 仍需继续收口真实路由切换、页面动作完整度与后台常用交互 |
| 字典 / 参数配置 | 平台支撑基础能力 | 🟡 部分完成 | 后端已闭环，前端已有字典与设置工作区 | `apps/server` + `apps/example-vue` | 仍缺更完整的后台交互、批量治理与导入导出 |
| 操作日志 / 登录日志 | 安全与运维基础视图 | 🟡 部分完成 | 操作日志列表/详情/导出已落地；登录相关事件当前混在 auth 审计记录内 | `apps/server` + `packages/persistence` | 缺独立登录日志视图与更接近日常后台的查询入口 |
| 通知 / 公告 | 基础消息触达 | 🟡 部分完成 | 站内通知已支持列表、详情、创建、标记已读 | `apps/server` + `apps/example-vue` | 仍未对齐“公告/通知”常见后台形态，多通道也未进入当前优先级 |
| 文件 / 附件 | 基础上传下载 | 🟡 部分完成 | 文件上传、下载、删除与元数据已落地 | `apps/server` + `apps/example-vue` | 还缺更完整的后台治理体验与批量操作能力 |
| 租户 / 数据权限 | 超出若依基础但已具备 | ✅ 已完成 | 多租户、RLS、数据权限与 tenant 管理已闭环 | `apps/server` + `packages/persistence` | 当前不作为阻断项，保持稳定即可 |
| 在线用户 / 会话治理 | 当前在线会话查看、强制下线 | 🟡 部分完成 | 后端已支持当前用户 refresh session 列表与单会话 revoke | `apps/server` + `packages/persistence` | 缺前端会话管理页、设备视图、批量或跨端强制下线 |
| 登录安全策略 | 登录失败计数、锁定窗口、解锁策略 | 🟡 部分完成 | 现有 token、refresh、RBAC、审计基线已具备 | `apps/server` | 缺失败计数、锁定、设备归一与更完整安全策略 |
| 岗位管理（post）/职位语义 | 用户-部门之外的基础组织能力 | ⚪ 未开始 | 当前仓库未落岗位模块 | 无 | 缺岗位模型、接口、权限点与前端工作区，也缺“岗位/职位”命名口径校准 |
| 导入导出 | 用户、字典、配置等高频模块的常用能力 | 🟡 部分完成 | 当前明确落地的导出主要在操作日志 | `apps/server` | 缺用户/字典/配置等模块的通用导入导出闭环 |
| 后台前端壳层 | 可切换、可工作的多模块后台 | 🟡 部分完成 | `ElyShell`、动态菜单与多工作区装配已存在 | `packages/ui-enterprise-vue` + `apps/example-vue` | 仍需继续收口页面切换、动作一致性与“不是演示页而是真实后台”的体验 |
| workflow 扩展 | transfer / delegate / 更通用 BPM | ⛔ 当前不优先 | `P7A Round-2` 最小闭环已完成 | `apps/server` + `packages/persistence` | 当前不继续外扩 |
| generator / SQL Studio 化 | 更完整的平台产品体验 | ⛔ 当前不优先 | 相关能力已有子矩阵与最小运行态 | `packages/generator` + `apps/server` | 当前降为次级轨道 |

## 当前优先顺序

### 第一优先级

1. 收口已有系统模块工作区，优先保证 `users / roles / menus / departments / dictionaries / settings / operation-logs / notifications / tenants` 具备真实后台闭环。
2. 补 `岗位管理（post）`，并在文档与界面层同步校准“岗位/职位”命名口径，避免基础组织能力长期缺位。
3. 补在线会话治理页、设备视图与强制下线，把已落后端 session 能力真正变成后台可用功能。
4. 补登录失败计数、锁定窗口与最小解锁策略。
5. 把导入导出从操作日志扩到用户、字典、参数等高频系统模块。

### 第二优先级

1. 补登录日志独立视图或在现有日志工作区内显式化 auth 安全事件。
2. 继续收口通知/公告的后台形态，但不提前进入多通道消息中心。
3. 继续提升文件、租户和数据权限工作区的日常可用性。

### 第三优先级

1. `generator / SQL` 的 Studio 化增强。
2. `workflow` 的更复杂任务语义。
3. 更重的平台化能力，如独立设计器、低代码 DSL、统一消息中心。

## 实施约束

- 优先复用既有 owner，不为了补齐功能矩阵新增第二套 shared helper 或 shared service。
- `apps/example-vue` 继续只做示例应用装配，不反向成为平台级路由框架 owner。
- workflow、generator、SQL 只做必要收口，不抢占当前第一优先级。
- 不把若依规划态功能写成“已实现事实”。

## 验证要求

- 新增后台基础能力必须至少有对应 server 合约测试或现有回归补充。
- `apps/example-vue` 的新增工作区收口不能破坏既有 `build:vue`。
- auth / session / permission 相关能力不得回退现有 401 / 403 / refresh / revoke 语义。

## 一句话结论

当前最优先的不是继续把平台做“更高”，而是先把它做“更像一个完整可用的企业后台”，优先朝若依的基础功能层对齐。
